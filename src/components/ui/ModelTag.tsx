export function ModelTag({ label }:{ label:string }) {
  return <span className="inline-block bg-s2 border border-border2 text-muted1 font-mono text-[10px] px-2.5 py-1 mr-1.5 mb-1.5">{label}</span>
}
